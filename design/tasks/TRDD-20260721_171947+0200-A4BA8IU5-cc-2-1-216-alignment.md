---
trdd-id: A4BA8IU5
title: Align ingest with Claude Code 2.1.204-2.1.216 — per-turn attribution, effort, new OTEL attrs
column: todo
created: 2026-07-21T17:19:47+0200
updated: 2026-07-21T17:19:47+0200
current-owner: main
task-type: feature
scope: project
implementation-commits: [d69a634]
relevant-rules: []
---

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-07-21

**Goal:** bring AgentlensPro's ingest in line with what Claude Code 2.1.204-2.1.216 now emits. The
changelog was the prompt; the transcripts were the evidence, and they carry MORE than the changelog
mentions.

**NEXT ACTION:** implement Gap A (per-turn skill/plugin attribution) — it is the highest-value item
and is independent of the rest. Start by deciding where it lands on `SessionSummaryCard` and whether
it needs a DB column or can ride the existing composition path.

**DONE (commit d69a634):** `/effort` added to the cache-risk vocabulary (7 real occurrences, maps to
the existing `EFFORT_CHANGED` cause); `/fork`, `/subtask`, `/branch`, `/resume` deliberately NOT
added, with a test pinning the omission; the command vocabulary moved to `src/shared/cacheRiskKinds.ts`
so `check-mirrors` guards it (113 → 117 protected exports).

## Gaps, measured against real transcripts (25,932 assistant records, newest 3 files)

| gap | field / change | evidence | value |
|---|---|---|---|
| **A** | `attributionSkill` (1,306) + `attributionPlugin` (1,108) on assistant records | present, **unread** | **HIGHEST — per-turn cost attribution to a named skill/plugin.** This is the product's whole purpose and nothing surfaces it today |
| **B** | `effort` on assistant records (CC 2.1.212) — 3,539 records, `xhigh`/`high` | present, **unread** by the transcript parser | makes `EFFORT_CHANGED` exact per-turn, not just at an explicit `/effort` |
| **C** | OTEL attrs `message.uuid`, `client_request_id`, `tool_source` (CC 2.1.214) | **unread**; note the transcript already carries `requestId` on 25,390 records | message-level correlation + tool provenance (builtin vs mcp vs plugin) |
| **D** | `CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH` / 60 KB content truncation (CC 2.1.214) | unmodelled | we may treat a truncated body as complete |
| **E** | per-session caps: subagents 200, WebSearch 200 (CC 2.1.212) | unsurfaced | burn-model context only — lowest value |

Also unread and cheap if Gap A touches the same parse site: `gitBranch` (per-turn branch, 25,932).

## Already aligned — verified, do NOT redo

- SessionStart `source: "fork"` → `FORK` (`src/lifecycleEvents.ts:19,38`). CC 2.1.214 changed forks
  from reporting `resume` to `fork`; our mapping already handles both, but historical rows before
  2.1.214 will read `RESUME` for what were actually forks — a data-interpretation caveat, not a bug.
- Opus 4.8 pricing (`src/shared/pricing.ts:85,89`), including the `-fast` variant.

## Deliberately out of scope, with the reason

- **`message_delta` cumulative double-count (CC 2.1.214).** We consume aggregated OTEL and parsed
  transcripts, never raw stream frames, so the double-count cannot reach us.
- **Prometheus `# UNIT` lines, Azure chunked-encoding rejection, `TRACEPARENT` trace_id.** Exporter-
  side fixes in Claude Code; nothing for a receiver to change.
- **`/fork`, `/subtask`, `/branch`, `/resume` as cache-risk commands.** Their cost lands in a
  DIFFERENT context than the session the command was typed in, so billing them to the parent's next
  turn would be a wrong number. If they are ever wanted, model them as session-SPAWN events with the
  child's cold start as the cost. Pinned by a test.
- Every Windows / PowerShell / worktree / agent-view / UI fix in the range.

## Method note (why this TRDD's numbers are trustworthy)

Each gap above was confirmed by scanning real transcripts, not by reading the changelog. That is
what caught Gap A — `attributionSkill`/`attributionPlugin` appear in NO release note in the range —
and what kept `/fork` and `/subtask` OUT despite the changelog featuring them prominently.

## Notes and lessons learned
